### server port
server:
  port: 8080

### database
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ssafy
    password: ssafy
    url: jdbc:mysql://localhost:3306/enjoytrip?serverTimezone=UTC&characterEncoding=UTF-8

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE